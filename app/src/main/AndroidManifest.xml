<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.android.transviro">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activity_Login_Screen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".RewardDashboard"
            android:label="@string/title_activity_reward_dashboard"
            android:theme="@style/AppTheme" />
        <activity android:name=".MainMenu" />
        <activity android:name=".PointsScreen" />
        <activity android:name=".SignUp" />
        <activity android:name=".LeaderboardScreen"></activity>
    </application>

</manifest>